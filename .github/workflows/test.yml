on:
  workflow_dispatch:
  
jobs:
  Start:
    name: Start
    runs-on: windows-latest
    
    steps: 
    - name: Start job
      run: |
        git clone https://github.com/AdminWhaleFall/SMSBoom.git/
        


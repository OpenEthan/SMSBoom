on:
  workflow_dispatch:
  push:
    branches:
    - main
    - release/*
  
jobs:
  Start:
    name: Start
    runs-on: windows-latest
    
    steps: 
    - name: Start job
      run: |
       curl https://github.com/AdminWhaleFall/SMSBoom/releases/download/test/smsboom_pyinstall.exe
       smsboom_pyinstall.exe
       
        

